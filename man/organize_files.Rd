% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/organize_files.R
\name{organize_files}
\alias{organize_files}
\title{Organize a directory of study files into a structured layout}
\usage{
organize_files(directory = NULL)
}
\arguments{
\item{directory}{Path to the directory to organize. Only files at the top
level are considered; existing subfolders are left untouched.}
}
\value{
\code{directory}, invisibly. Called for its side effects on the file
system.
}
\description{
Sorts the files at the top level of \code{directory} into subfolders by type,
renaming each file to a cleaned, snake_case table name. Before moving
anything, a key mapping original file names to their new names is written to
\code{.abcdsmeta/file_key.csv}, which allows the operation to be reversed with
\code{\link[=restore_original_files]{restore_original_files()}}.
}
\details{
Files are routed by matching their name against a fixed set of patterns, in
this order:

\describe{
\item{\verb{controls/}}{name contains "controls"}
\item{\verb{codebook/}}{name contains "dictionary" or "codebook"}
\item{\verb{documents/}}{\code{.pdf} or \code{.docx} extension}
\item{\verb{participants/}}{\code{.csv} extension}
}

The first match wins, so the order is deliberate: a file named
\code{data_dictionary.pdf} is routed to \verb{codebook/}, not \verb{documents/}.
}
\section{Side effects}{

Creates \verb{.abcdsmeta/} and one subfolder per matched destination, writes
\code{.abcdsmeta/file_key.csv}, and moves the matched files off the top level.
The key is written before any file is moved, so a partial failure can still
be recovered with \code{\link[=restore_original_files]{restore_original_files()}}.
}

\section{Errors}{

Aborts without modifying the directory if \code{.abcdsmeta/file_key.csv} already
exists (a signal the directory has already been organized), if two files
clean to the same table name, or if any file matches none of the routing
patterns. Warns and returns early if there is nothing to organize.
}

\seealso{
\code{\link[=restore_original_files]{restore_original_files()}} to undo the result.
}
