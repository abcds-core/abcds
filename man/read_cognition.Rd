% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/read_abcds.R
\name{read_cognition}
\alias{read_cognition}
\title{Read cognition data from ABCDS study}
\usage{
read_cognition(type, directory = NULL, controls, add_demographics = TRUE)
}
\arguments{
\item{type}{Character string or unquoted name. Type of imaging data to read.
Options: \code{dsmse}, \code{recall}, \code{haxby}, \code{wisc}, \code{catdog}, \code{purude}, \code{tinetti},
\code{vmi}, \code{verbal}, \code{radd}, \code{moca}}

\item{directory}{Character string. Path to the directory containing data files.
If NULL, uses the path set by \code{\link[=set_abcds_directory]{set_abcds_directory()}}. Default is NULL.}

\item{controls}{Logical. If TRUE, reads control data; if FALSE, reads
participant data. Default is FALSE.}

\item{add_demographics}{Logical. If TRUE, merges demographic data
(age, gender, race, ethnicity) with the returned data. Default is TRUE.}
}
\value{
A tibble containing the requested imaging data, optionally merged
with demographics.
}
\description{
Reads various cognitive function assessments from the ABCDS study.
}
\examples{
\dontrun{
# Read Mental Status Exam data (quoted)
dsmse_data <- read_cognition("dsmse")

# Read cued recall data (unquoted)
recall_data <- read_cognition(recall)

# Specify custom directory
catdog_data <- read_cognition(catdog, directory = "path/to/data")
}
}
