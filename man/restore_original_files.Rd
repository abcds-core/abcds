% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/organize_files.R
\name{restore_original_files}
\alias{restore_original_files}
\title{Restore an organized directory to its original file layout}
\usage{
restore_original_files(directory = NULL)
}
\arguments{
\item{directory}{Path to a directory previously processed by
\code{\link[=organize_files]{organize_files()}}.}
}
\value{
\code{directory}, invisibly. Called for its side effects on the file
system.
}
\description{
Reverses \code{\link[=organize_files]{organize_files()}}. Reads \code{.abcdsmeta/file_key.csv}, copies each
organized file back to the top level of \code{directory} under its original name,
and then removes the subfolders created during organization.
}
\section{Side effects}{

Copies files back to the top level of \code{directory}, overwriting any existing
file of the same name, then deletes the \verb{documents/}, \verb{controls/},
\verb{codebook/}, \verb{participants/}, and \verb{.abcdsmeta/} subfolders. Deletion only
happens once every copy has been confirmed, so a failed restore leaves the
organized layout intact.
}

\section{Errors}{

Aborts if \code{.abcdsmeta/file_key.csv} is missing, if any organized file has no
corresponding entry in the key, or if any copy fails. In the latter two
cases nothing is deleted.
}

\seealso{
\code{\link[=organize_files]{organize_files()}}, which produces the layout this function undoes.
}
